Build a polished, working cybersecurity portfolio project called SentinelScope: a web security posture assessment dashboard. Use a modern full-stack TypeScript stack that runs in Replit. Make it look professional enough to demo to cybersecurity recruiters, with responsive dark UI, clear visual hierarchy, accessible controls, and realistic sample data.

Core workflow: user enters a domain they own or are authorized to assess, runs a bounded read-only assessment, views findings grouped by severity, and exports a clean HTML or PDF report. Include a built-in Demo Assessment so the app is useful immediately without scanning a live site.

Checks: HTTPS/TLS certificate validity and expiry; HTTP-to-HTTPS redirect; security headers (CSP, HSTS, X-Content-Type-Options, Referrer-Policy, Permissions-Policy, frame protection); cookie Secure/HttpOnly/SameSite flags; mixed content on the landing page; robots.txt and security.txt presence. Explain what each finding means, evidence observed, and a concrete remediation. Show overall score, severity counts, check history, and a comparison of two saved assessments.

Security requirements: require an explicit authorization checkbox before a live assessment; only GET/HEAD requests to the supplied public domain; no brute force, port scans, authentication attempts, or vulnerability exploitation. Prevent SSRF: block localhost, private/reserved IPs and hostnames; recheck DNS and redirects; enforce timeouts, response-size limits, and rate limits. Keep all probing server-side. Do not persist secrets or sensitive page contents. Make clear in the UI that results are informational and scoped to the checks performed.

Deliver a complete app, not mock screens: working routes and components, persistent scan history, seed demo assessment, useful empty/loading/error states, export, and a README explaining architecture, safe usage, setup, test commands, screenshots to capture, and how to discuss the project on a cybersecurity resume. Add focused tests for scoring and SSRF guard. Run the app and tests, fix failures, and show me the finished preview. Avoid paid third-party APIs.